# Copy to apps/api/.env for local development.

ENVIRONMENT=development

# Async driver is required (asyncpg), not plain postgresql://
DATABASE_URL=postgresql+asyncpg://postgres:postgres@127.0.0.1:5432/learnpython

# Session cookie
SESSION_COOKIE_NAME=lp_session
SESSION_TTL_DAYS=30
